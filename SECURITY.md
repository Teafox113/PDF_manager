# 安全性說明

## 文件處理

PDF Manager 的文件內容在瀏覽器本機處理。程式沒有用來上傳文件的伺服器端 API。

開發版會向 cdnjs 載入固定版本的第三方函式庫；離線版已內嵌函式庫並限制外部連線。

## 敏感資料

請勿在儲存庫中提交：

- 真實客戶或個人 PDF、圖片、DOCX
- `.env`、API Key、Token、密碼或私鑰
- 內部網路位址、個資或機密測試資料

上述類型已由 `.gitignore` 部分防護，但提交前仍須人工檢查。

## 回報問題

若儲存庫日後公開，安全問題應優先使用 GitHub 的私人安全性通報功能。請勿在公開 Issue 中附上機密文件或可用憑證。

## 目前加固建議

- 建置時可再加入第三方 CDN 檔案的雜湊驗證。
- DOCX 預覽流程可再加入內容淨化或 iframe sandbox。
- 發布前應在斷網環境驗證離線版沒有外部請求。

